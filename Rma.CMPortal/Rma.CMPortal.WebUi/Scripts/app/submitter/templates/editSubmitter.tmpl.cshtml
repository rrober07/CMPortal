@using HeroicCRM.Web.Helpers
@model HeroicCRM.Web.Models.EditSubmitterForm
@{
	var customer = Html.Angular().ModelFor("vm.submitter");
}
<form novalidate
	  name="vm.form"
	  ng-submit="vm.form.$valid && vm.save()">
	<fieldset ng-disabled="vm.saving">

		<div class="modal-header">
			<h3 class="modal-title">Edit Submitter</h3>
		</div>

		<div class="modal-body">

			<div class="alert alert-info" ng-hide="vm.errorMessage != null">
				Make changes to the submitter below.
			</div>
			<div class="alert alert-danger" ng-show="vm.errorMessage != null">
				{{vm.errorMessage}}
			</div>

			@Html.Angular().FormForModel("vm.submitter")

		</div>

		<div class="modal-footer">
			<button class="btn btn-success">Save</button>
			<button type="button" class="btn btn-warning" ng-click="$parent.$dismiss()">Cancel</button>
		</div>

	</fieldset>
</form>